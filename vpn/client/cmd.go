package client

// figure out flag package and design skeleton version cli

func StartCmd() {
	// ip := GetIPAddr()
	// port := NODE_PORT
	// 
	// req := FormRequest(ip, port)
	// SendRequestToCoordServer(req)
}
